[variables]
main_domain = "${domain}"

[config]
[[config.domains]]
serviceName = "actualbudget"
port = 5006
host = "actualbudget.${main_domain}"

[config.env]
# Database Configuration (connect to external Supabase)
POSTGRES_URL = "${{project.POSTGRES_URL}}"

# S3 Configuration (using project variables)
AWS_ACCESS_KEY_ID = "${{project.S3_ACCESS_KEY_ID}}"
AWS_SECRET_ACCESS_KEY = "${{project.S3_SECRET_ACCESS_KEY}}"
AWS_REGION = "${{project.S3_REGION}}"
AWS_BUCKET = "${{project.S3_BUCKET}}"
AWS_S3_FORCE_PATH_STYLE = "true"

# Application Configuration
HTTP_PORT = "5006"
ACTUAL_TRUST_PROXY = "1"

# Password Configuration
PASSWORD_ENCRYPTION_KEY = "${password:32}"

[[config.mounts]]
volumeName = "actualbudget-data"
mountPath = "/app/data"

[[config.mounts]]
volumeName = "actualbudget-uploads"
mountPath = "/app/uploads"